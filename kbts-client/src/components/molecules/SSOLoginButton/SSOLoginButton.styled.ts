'use client';

import { styled } from 'styled-components';

import { Box } from '@Atoms/Box';

export const SSOLoginButtonIconBox = styled(Box)`
    position: absolute;
    align-self: flex-start;
    align-items: center;
    width: 27px;
    height: 27px;
`;
